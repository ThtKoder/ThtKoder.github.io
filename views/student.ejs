<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/ref.css">
    <script src="https://github.com/ThtKoder/ThtKoder.github.io/blob/main/student.js"></script>
    
    <link rel="stylesheet" href="https://code.jquery.com/ui/1.14.1/themes/base/jquery-ui.css">
    <link rel="stylesheet" href="/resources/demos/style.css">
    <script src="https://code.jquery.com/jquery-3.7.1.js"></script>
    <script src="https://code.jquery.com/ui/1.14.1/jquery-ui.js"></script>

</head>
<body>
    <img style="width: 100%; height: 100%" src="/assets/Cal25-26.jpg" />

    <div data-has-description="false" data-has-error="false" data-has-label="true" data-state="Default" data-value-type="Default" style="width: 100%; height: 100%; flex-direction: column; justify-content: flex-start; align-items: flex-start; gap: 8px; display: inline-flex"></div>
    <!-- border/margin -->

    <criterions>

        <div data-has-description="false" data-has-error="false" data-has-label="true" data-state="Default" data-value-type="Default" style="width: 100%; height: 100%; flex-direction: column; justify-content: flex-start; align-items: flex-start; gap: 8px; display: inline-flex">

        <!-- Criterion A -->
        <criterion>Criterion A: Describing a Problem</criterion>
        <smalldiv></smalldiv>
        <criterioncontent>"One problem that we have noticed around..."</criterioncontent>

        <p>Crit A Date: <input type="text" id="datepickerA"></p>

        <div data-has-description="false" data-has-error="false" data-has-label="true" data-state="Default" data-value-type="Default" style="width: 100%; height: 100%; flex-direction: column; justify-content: flex-start; align-items: flex-start; gap: 8px; display: inline-flex"></div>

        <!-- Criterion B -->
        <criterion>Criterion B: Developing a Solution</criterion>
        <smalldiv></smalldiv>
        <criterioncontent>"We plan to address the above problem by..."</criterioncontent>

        <p>Crit B Date: <input type="text" id="datepickerB" class="has"></p>

        <div data-has-description="false" data-has-error="false" data-has-label="true" data-state="Default" data-value-type="Default" style="width: 100%; height: 100%; flex-direction: column; justify-content: flex-start; align-items: flex-start; gap: 8px; display: inline-flex"></div>

        <!-- Criterion C -->
        <criterion>Criterion C: Creating a Solution</criterion>
        <smalldiv></smalldiv>
        <criterioncontent>"We seek to create a solution that is..."</criterioncontent>

        <p>Crit C Date: <input type="text" id="datepickerC"></p>

        <div data-has-description="false" data-has-error="false" data-has-label="true" data-state="Default" data-value-type="Default" style="width: 100%; height: 100%; flex-direction: column; justify-content: flex-start; align-items: flex-start; gap: 8px; display: inline-flex"></div>

        <!-- Criterion D -->
        <criterion>Criterion D: Evaluating</criterion>
        <smalldiv></smalldiv>
        <criterioncontent>"We believe that success is defined as..."</criterioncontent>

        <p>Crit D Date: <input type="text" id="datepickerD"></p>

        <div data-has-description="false" data-has-error="false" data-has-label="true" data-state="Default" data-value-type="Default" style="width: 100%; height: 100%; flex-direction: column; justify-content: flex-start; align-items: flex-start; gap: 8px; display: inline-flex"></div>

    </criterions>

    <button onclick="saveText()">Save Text</button>
    
    <div i="dropzone" style="width: 100%; height: 200px; border: 2px dashed #ccc; display: flex; justify-content: center; align-items: center;">
        <p id="dropzone-msg">Drag and drop your file here</p>

    <script>
        const dropzone = document.getElementById('dropzone');
        const dropzoneMsg = document.getElementById('dropzone-msg');

        dropzone.addEventListener('dragover', (event) => {
            event.preventDefault();
        });

        dropzone.addEventListener('drop', () => {
            event.preventDefault();

            if( event.dataTransfer.files.length > 0) {
                const file = event.dataTransfer.files[0];
                dropzoneMsg.textContent = `File "${file.name}" dropped successfully!`;
                

                // Here you can add code to handle the file, like uploading it to a server
            } else {
                dropzoneMsg.textContent = 'No files dropped.';
            }
        });

    </script>

</body>
</html>